#include <stdio.h>
#include <stdlib.h>

int main()
{
   int a,b,c;

   printf("Kerem a haromszog oldalait:");
   scanf("%d %d %d",&a,&b,&c);
   {if (a==c && a==b && b==c)
       printf("egyenlo oldalu haromszog");
   else

   if(b==c,c==a,a==b) printf("egyenloszaru haromszog");

   else

   printf("egyenlotlen szaru haromszogek");

   }
    return 0;
}
