#include <stdio.h>
#include <stdlib.h>


int main()
{
    int x,y,z;

    printf("adj meg 3 szamot:");
    scanf("%d %d %d",&x, &y, &z);
    printf("Elso szám:%d       Masodik szam:%d     Haramdik szam:%d\n",x, y, z);


{   if (z >= x && z >= y )
        printf("%d a legnagyobb\n",z);
        else
    if (z >= x && z >= y )
        printf("%d a legnagyobb\n",z);
        else
    if (y >= x && y >= z )
        printf("%d a legnagyobb\n",y);}


    return 0;
}
