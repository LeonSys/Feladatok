#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b;
    printf("Kerek 2 szamot\n");
    scanf("%d %d", &a, &b);
    { if (a == b)

        printf("a ket szam egyenlo");
         else
        printf(" a ket szam nem egyenlo");
    }

    return 0;
}
